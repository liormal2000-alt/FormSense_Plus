# Required for the optional AI coaching summary and recommendations.
GEMINI_API_KEY=replace_with_your_key

# Optional. The application defaults to the stable Gemini 2.5 Flash model.
GEMINI_MODEL=gemini-2.5-flash
