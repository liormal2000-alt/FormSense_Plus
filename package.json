{
  "name": "formsense-plus",
  "version": "2.0.0",
  "private": true,
  "description": "Browser-based exercise-form analysis with MediaPipe Pose, deterministic biomechanics, and confidence-aware AI coaching.",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "test": "node --test tests/*.test.js",
    "check": "npm test"
  },
  "devDependencies": {
    "netlify-cli": "^27.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
